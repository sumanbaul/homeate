import 'package:flutter/material.dart';

class ColorSchemes {
  Color backgroundColor, shadowColor, textColor;
  themeWhite() {
    backgroundColor = Colors.grey[300]; //Color(0xFFF1f2f6);
    shadowColor = Color(0xFFDADFF0);
    textColor = Color(0xFF707070);
  }
}

Color backgroundColor = Colors.grey[300]; //Color(0xFFF1f2f6);
Color shadowColor = Color(0xFFDADFF0);
Color textColor = Color(0xFF707070);
